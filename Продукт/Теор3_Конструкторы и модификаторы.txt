 онструкторы Java.
	 онструктор в Java - это специальный метод, который используетс€ дл€ инициализации объектов.  онструктор вызываетс€ при создании объекта класса. ≈го можно использовать дл€ установки начальных значений атрибутов объекта.»м€ конструктора должно совпадать с именем класса и не может иметь возвращаемого типа (например, void).

public class Main {
  int x;  
  
  public Main() {
    x = 5;  attribute x
  }

  public static void main(String[] args) {
    Main myObj = new Main();
    System.out.println(myObj.x); 
  }
}

	 онструкторы также могут принимать параметры, которые используютс€ дл€ инициализации атрибутов.
ѕример:¬нутри конструктора мы устанавливаем x в y (x = y).  огда мы вызываем конструктор, мы передаем параметр конструктору (5), который устанавливает значение x равным 5:

public class Main {
  int x;

  public Main(int y) {
    x = y;
  }

  public static void main(String[] args) {
    Main myObj = new Main(5);
    System.out.println(myObj.x);
  }
}


ћодификаторы.
	¬ Java существует 4 модификатора доступа
private : доступ в текущем класса
default(package-private) : private + доступ внутри пакета
protected : default + доступ внутри иерархии классов
public : доступен всем 

ƒл€ классов , то вы можете использовать public или :

public	 ласс доступен дл€ любого другого класса	
default	класс доступен только классам в том же пакете. ќн используетс€, когда вы не указываете модификатор.

ƒл€ атрибутов, методов и конструкторов вы можете использовать одно из следующих:

public	 ласс доступен дл€ любого другого класса
private	 од доступен только внутри объ€вленного класса
default	 ласс доступен только классам в том же пакете. ќн используетс€, когда вы не указываете модификатор.
protected ƒоступен в том же пакете и подклассах.