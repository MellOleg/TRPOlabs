ѕерегрузка метода.
	¬ программе мы можем использовать методы с одним и тем же именем, но с разными типами и/или количеством параметров. “акой механизм называетс€ перегрузкой методов. —тоит отметить, что на перегрузку методов вли€ют количество и типы параметров. ќднако различие в типе возвращаемого значени€ дл€ перегрузки не имеют никакого значени€.

public class Program{
      
    public static void main(String[] args) {
         
        System.out.println(sum(2, 3));          // 5
        System.out.println(sum(4.5, 3.2));      // 7.7
        System.out.println(sum(4, 3, 7));       // 14
    }
    static int sum(int x, int y){
             
        return x + y;
    }
    static double sum(double x, double y){
             
        return x + y;
    }
    static int sum(int x, int y, int z){
             
        return x + y + z;
    }
}